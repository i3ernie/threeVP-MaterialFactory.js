/**
 * 
 * @param {type} THREE
 * @param {type} _
 * @param {type} $
 * @param {type} Backbone
 * @param {type} CMD
 * @returns {packL#5.packAnonym$1}
 */

define([ "factorys/MaterialFactory"], 
function( MaterialFactory ) {
    return {
        MaterialFactory       : MaterialFactory
     };
});
